""" Computes the sum of x numbers """

x = int(program.args[1])
inputs = sint.Array(x)
#temp = sint(0)

@for_range(x)
def _(i):
    inputs[i] = sint.get_input_from(i)
    ''' Nicht secret values revealen
    if i != x:
        print_str('%s + ', temp.reveal())
    else:
        print_str('%s = ', temp.reveal())
    '''

result = sum(inputs)

print_str('%s\n', result.reveal())
